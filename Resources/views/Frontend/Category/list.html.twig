<ul>
    {% for category in categories %}
    
        <li><a href="{{ path('sylius_catalog_category_show', {'id': category.id, 'slug': category.slug}) }}">{{ category.name }}</a></li>
    
    {% endfor %}
</ul>